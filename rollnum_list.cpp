#include <bits/stdc++.h>
using namespace std;

int main()
{
	roll_num = []
	marks = []
	count=0;
	ranklist=open("ranklist.txt","r")
	for word in ranklist.read().split() :
		if (count % 2 == 0) :
			roll_num.append(word)
		else :
			marks.append(word)
		count += 1


	for i in marks :
		print(i," ")









	return 0;
}
